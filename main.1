#include <iostream>
#include <string>

void drawRectangle(int height, int width, char symbol) {
  std::string row(width, symbol); 
  for (int i = 0; i < height; i++) {
    std::cout << row << std::endl; 
  }
}

int main() {
  drawRectangle(5, 10, '*'); 
  return 0;
}

#include <iostream>
using namespace std;

int max(int num1, int num2) {
    if (num1 > num2)
        return num1;
    else
        return num2;
}

int main() {
    int a = 5;
    int b = 10;
    int largest = max(a, b);
    cout << "The largest number is " << largest << endl;
    return 0;
}
